$kind: http-request
name: Reject Seller Application
method: POST
url: '{{baseUrl}}/superadmin/seller/reject'
order: 3000
body:
  type: urlencoded
  content:
    - key: userId
      value: '2'
      description: ID of the user whose seller application to reject
